#include <iostream>
using std::cin;
using std::cout;
using std::endl;

int main() {
    int a = 40;
    int b = 30;
    if (a < b){
        cout << "a is smaller than b " << endl;
    }
    else if (a == b)
    {
        cout << " a is equal to b " << endl;
    }
    else if (a > b)
    {
        cout << " a is not smaller than b " << endl;
    }
}
